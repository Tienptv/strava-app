import fs from 'fs'; import path from 'path'; import { fileURLToPath } from 'url'; const __filename = fileURLToPath('file:///c:/Users/926166/OneDrive - Haskoning/Tien_926166/Strava_Desktop_Software/server/index.js'); const __dirname = path.dirname(__filename); const scripts = []; const scanDir = (dir) => { try { const entries = fs.readdirSync(dir, { withFileTypes: true }); for (let entry of entries) { if (entry.name === 'node_modules' || entry.name === 'dist' || entry.name === 'desktop_release' || entry.name === '.git') continue; const fullPath = path.join(dir, entry.name); if (entry.isDirectory()) { scanDir(fullPath); } else if (entry.name.toLowerCase().endsWith('.bat')) { scripts.push({ name: entry.name, path: fullPath, relativePath: path.relative(path.join(__dirname, '..'), fullPath) }); } } } catch(e) { console.error('Error reading ' + dir + ': ' + e.message); } }; scanDir(path.join(__dirname, '..')); console.log('Found scripts:', scripts.length);
