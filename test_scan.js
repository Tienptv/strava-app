const fs = require('fs'); const path = require('path'); const scripts = []; const scanDir = (dir) => { const entries = fs.readdirSync(dir, { withFileTypes: true }); for (let entry of entries) { if (entry.name === 'node_modules' || entry.name === 'dist' || entry.name === 'desktop_release' || entry.name === '.git') continue; const fullPath = path.join(dir, entry.name); if (entry.isDirectory()) { scanDir(fullPath); } else if (entry.name.toLowerCase().endsWith('.bat')) { scripts.push({ name: entry.name, path: fullPath }); } } }; try { scanDir('c:/Users/926166/OneDrive - Haskoning/Tien_926166/Strava_Desktop_Software'); console.log(scripts); } catch(e) { console.error(e.message); }
